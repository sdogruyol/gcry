/lib/
/bin/
/.shards/
*.dwarf
.tools
.crystal-cache
.crystal_cache
.cache
.DS_Store

# Local scratch (acik rebuild bins, probe trees)
.tmp/
.tmp-*/

# Local bench Postgres / Cursor project bits (not ship)
.bench-pg/
.cursor/

# Libraries don't need dependency lock
# Dependencies will be locked in applications that use them
/shard.lock

# Nested Kemal bench app deps
/bench/kemal/lib/
/bench/kemal/.shards/
/bench/kemal/shard.lock

# Nested crystal-metric bench deps / upstream results cache
/bench/crystal_metric/lib/
/bench/crystal_metric/.shards/
/bench/crystal_metric/shard.lock
/bench/crystal_metric/results.js

# Generated perf artifacts (not cross-host baselines)
/bench/trend.json
/bench/mutations/SCORE.log
# `make thread-uaf-sample` output: evidence from a CI run, not a source file.
bench/log/ci-samples/
