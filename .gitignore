/lib/
/bin/
/.shards/
*.dwarf
.tools
.crystal-cache

# Libraries don't need dependency lock
# Dependencies will be locked in applications that use them
/shard.lock

# Nested Kemal bench app deps
/bench/kemal/lib/
/bench/kemal/.shards/
/bench/kemal/shard.lock
