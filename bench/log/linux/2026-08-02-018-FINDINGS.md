# 0.18 shard-only campaign FINDINGS

Parent Parallel hub:
[2026-07-29-parallel-tlab-FINDINGS.md](2026-07-29-parallel-tlab-FINDINGS.md).

**Tag policy:** no `v0.18.0` until EC1 `/json` thr clears a real bar
(soft ≥90% @ ≤0.85× RSS on the measuring host; hard ≥95% @ ≤1.0×).
Pause/CI alone is not a release. Product version stays **v0.17.0**.

## Gates

| Gate | Result |
|------|--------|
| EC1 `/json` ≥95% @ RSS ≤1.0× | **MISS** |
| Soft ≥90% @ ≤0.85× (9950X hunt) | **MISS** — best default-path ~**82%** @ **0.76×**; KEEP escape **90%** @ **3.2×** |
| Parallel ≥75% @ ≤4× RSS | **HIT as opt-in** (`GCRY_PARALLEL_DORMANT=1`); default-on **rejected** |
| KEEP_CHUNKS as default | **REJECT** — 90–95% @ ~3× RSS |

## Host: Ryzen 9 9950X (WSL2) — tip cite

Crystal 1.21.0 · tip · EC1 · `wrk -c100 -d30` · med-of-3 · post-GC RSS.
Pause campaign on tip (post-STW sweep; pause_p50 ~**0.33 ms**).

| Session | Role | `/json` % | RSS × | pause_p50 |
|---------|------|----------:|------:|----------:|
| `2026-08-03-072122/` | quiet baseline | **82.5%** | **0.76×** | **0.33 ms** |
| `2026-08-03-072954/` | soft confirm | **80.3%** | **0.76×** | **0.33 ms** |

Band: `/json` ~**80–83%** @ **0.76×**. Idle `/` host-noise. Do not mix with i3.

EC4 soft-soak: **40/40 soft=0 hard=0** (`make soft-soak-ec4`).

## Phase 0 baselines (i3-12100F, parked)

| Session | Config | `/json` % | RSS × |
|---------|--------|----------:|------:|
| `2026-08-02-120500/` | EC1 default | **87.9%** | **0.81×** |
| `2026-08-02-145600/` | EC4 lazy reclaim-off | **80.5%** | **5.48×** |

## Phase 1 levers (i3)

| Lever | `/json` | RSS × | Verdict |
|-------|--------:|------:|---------|
| `GCRY_KEEP_CHUNKS=1` (`121411/`) | **95.0%** | **3.07×** | escape only |
| retain 32 MiB (`122030/`) | **81.9%** | **0.88×** | **reject** (thr↓) |
| hot-prefer dormant demote (`150536/`) | **84.5%** | **0.81×** | **reject** |
| `GCRY_ALLOC_BATCH=4` | — | — | incomplete on i3 |

## Phase 2 (i3)

| Lever | `/json` | RSS × | Verdict |
|-------|--------:|------:|---------|
| Parallel dormant **default-on** + retain 32 (`151303/`) | **68.8%** | **3.29×** | **reject default** |
| Prior opt-in dormant+lazy retain 32 | **75.1%** | **4.03×** | **keep supported opt-in** |

## Phase 3 — 9950X thr hunt **CLOSED (MISS)**

Hub detail: [2026-08-03-9950x-thr-hunt/](2026-08-03-9950x-thr-hunt/).

| Session | Config | `/json` % | RSS × | Verdict |
|---------|--------|----------:|------:|---------|
| `2026-08-03-080248/` | `GCRY_KEEP_CHUNKS=1` | **90.1%** | **3.23×** | escape only (soft thr hit; RSS fail) |
| `2026-08-03-081125/` | `GCRY_ALLOC_BATCH=4` | — | — | **REJECT** — SEGV under `/json` wrk |
| `2026-08-03-081646/` | warm retain 32 MiB | **82.0%** | **2.86×** | **reject** (no thr↑; RSS↑) |
| `2026-08-03-082409/` | warm retain 256 MiB | **87.0%** | **3.19×** | **reject default** (≈KEEP shape; RSS fail; <90%) |

### Phase profile (9950X)

See [phase-profile.md](2026-08-03-9950x-thr-hunt/phase-profile.md).

- Mark/roots/scrub not the gap.
- Default munmaps ~5 GB over 30s wrk; KEEP collapses flush → +~8–10 pp thr @ ~3× RSS.
- Warm retain (mapped empties, no DONTNEED) does **not** open a ≤0.85× RSS path to ≥90%. Bounded warm pays RSS without KEEP thr; large warm ≈ KEEP.

### `GCRY_EMPTY_CHUNK_WARM_RETAIN`

Opt-in research knob (not default): bytes of fully-free chunks kept
mapped+warm before dormant/munmap. Measured; **not** a process default.

## Reconfirm after retain=0 defaults (2026-08-04)

| Session | Config | `/json` % | RSS × | Verdict |
|---------|--------|----------:|------:|---------|
| `2026-08-04-042404/` | tip default (retain=0) | **85.0%** | **0.78×** | soft/hard still **MISS** (≈ hunt band) |
| `2026-08-04-043747/` | `KEEP_CHUNKS=1` | **92.6%** | **3.25×** | escape only (soft thr; RSS fail) |

`unmapped_bytes` still ~7.5 GB/trial on default. Shipping Linux retain=0 did
**not** open ≥90%@≤0.85×.

## Campaign outcome — shard-only thr **exhausted**

- Tip pause win (~4→~0.3 ms) real; does not move thr headline → **no tag**.
- 9950X re-open: KEEP ceiling, ALLOC_BATCH, warm middle path — **no**
  default-path ≥90% @ ≤0.85×.
- 2026-08-04 reconfirm after finalizer + retain=0: still **~85%** @ **~0.78×**.
- **Escalate:** next real bet is **compiler stack maps** / precise roots
  (ROADMAP Phase 2) — not another shard-only retain knob.
